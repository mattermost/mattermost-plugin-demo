import Manifest from './manifest';

// Namespace your actions to avoid collisions.
export const STATUS_CHANGE = Manifest.PluginId + '_status_change';

export const OPEN_ROOT_MODAL = Manifest.PluginId + '_open_root_modal';
export const CLOSE_ROOT_MODAL = Manifest.PluginId + '_close_root_modal';
